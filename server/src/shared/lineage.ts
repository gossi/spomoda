import { Aggregate, App } from 'wolkenkit';

export function getDescendents(skill: Aggregate, app: App) {
	const descendents = [];

	// for (const id of skill.parents) {
	// 	app.sport.skill(skill.id).read();
	// }

}
