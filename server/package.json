{
	"name": "@spomoda/server",
	"version": "1.0.0",
	"description": "spomoda",
	"keywords": [
		"spomoda"
	],
	"license": "MIT",
	"scripts": {
		"transpile": "babel src --out-dir server --extensions '.ts,.js'",
		"prepare": "yarn transpile; cp -R res/* server/",
		"start": "yarn prepare; wolkenkit start; rm -rf server",
		"reload": "yarn prepare; wolkenkit reload; rm -rf server"
	},
	"wolkenkit": {
		"application": "spomoda",
		"runtime": {
			"version": "3.0.0"
		},
		"environments": {
			"default": {
				"api": {
					"address": {
						"host": "local.wolkenkit.io",
						"port": 3000
					},
					"allowAccessFrom": "*"
				},
				"fileStorage": {
					"allowAccessFrom": "*"
				},
				"identityProvider": {
					"name": "https://gossi.eu.auth0.com/",
					"certificate": "/server/keys/auth0"
				},
				"node": {
					"environment": "development"
				}
			}
		}
	},
	"dependencies": {
		"slugify": "^1.3.4",
		"wolkenkit-command-tools": "^3.0.0"
	},
	"devDependencies": {
		"@babel/cli": "^7.2.3",
		"@babel/core": "^7.2.2",
		"@babel/preset-env": "^7.2.3",
		"@babel/preset-typescript": "^7.1.0"
	}
}
