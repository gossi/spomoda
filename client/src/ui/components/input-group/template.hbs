<FormGroup @label={{@label}} @description={{@description}} @error={{@error}} @id={{this.id}} ...attributes as |id|>
	<Input
		@id={{id}}
		@type={{this.type}}
		@value={{@value}}
		@placeholder={{@placeholder}}
		@changed={{action (optional @changed)}}
		local-class="input" />
</FormGroup>