<FormGroup @label={{@label}} @description={{@description}} @error={{@error}} @for={{this.id}}>
	<Text
		@id={{this.id}}
		@type={{this.type}}
		@value={{@value}}
		@placeholder={{@placeholder}}
		@changed={{action (optional @changed)}}
		local-class="input"/>
</FormGroup>