<label local-class="label">
	<input type="checkbox" checked={{@checked}} {{on "change" this.change}}>

	{{@label}}

	{{#if @description}}
		<small local-class="description">{{@description}}</small>
	{{/if}}
</label>