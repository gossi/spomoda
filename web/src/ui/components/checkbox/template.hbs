<label local-class="label">
	<input type="checkbox" checked={{@value}} onchange={{bind this.change}}>

	{{@label}}

	{{#if @description}}
		<small local-class="description">{{@description}}</small>
	{{/if}}
</label>